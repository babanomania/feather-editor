# Implementation Plans

Generated by the `/improve` skill on 2026-06-17 against commit `67d9297` (branch `audit/improve-2026-06-17`).

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish a verification baseline with `node --test` | P1 | M | — | DONE |
| 002 | Enforce bundle-size budgets in the publish gate | P1 | S | 001 (soft) | DONE |
| 003 | Sanitize URLs in the link toolbar prompt and Markdown link parser | P1 | S | 001 | TODO |
| 004 | Document the editor's trust boundary in README and CLAUDE.md | P2 | XS | 003 | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with one-line reason) | `REJECTED` (with one-line rationale).

## Dependency notes

- **002** is independent of 001 in code terms but the size guard is harder to trust without a way to verify the bundles actually work. Run 001 first; 002 can land second.
- **003** depends on 001 because the XSS fix needs the test harness to verify it landed correctly. The test cases for `safeUrl` are part of plan 003's own work, but they rely on the runner that 001 sets up.
- **004** depends on 003 — the README wording promises what `safeUrl` does, which doesn't exist until 003 ships.

## Findings considered and rejected

- **`light.css` doubles as base + light palette** — documented design choice in `CLAUDE.md`. Not a finding.
- **`document.execCommand` is deprecated** — `CLAUDE.md` explicitly settles this as intentional given the byte-budget. Revisit only if a major browser actually breaks it.
- **No CI** — surfaced in the audit; deliberately not planned in this batch because it's blocked on plan 001 landing tests for CI to run. Pick it up in a follow-on audit once 001 is `DONE`.
- **esbuild 0.21 → 0.28 upgrade (`npm audit` high)** — dev dep only, no user impact. Mechanical bump can be tackled any time; not high enough leverage to occupy a P1 slot.
- **Hoist inner `B` component** — same root cause as a future "shave bytes" plan. Better to revisit once plan 002's size guard is in place so the byte delta can be tracked.

## Direction options (from the audit, not planned)

These are forward-looking; they require a maintainer decision before becoming plans.

- **DIR-A**: Single bundled `themes/all.css` for one-line theming.
- **DIR-B**: Standalone (no-React) build, productizing the vanilla version already inlined in `index.html`.
- **DIR-C**: Opt-in `feather-editor/safe` subpath that pre-sanitizes input.
